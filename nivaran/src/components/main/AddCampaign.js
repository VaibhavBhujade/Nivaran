import React, { Component } from "react";
import {
    Card, CardImg, CardText, CardBody,
    CardTitle, CardSubtitle, Button
  } from 'reactstrap';

class Campaign extends Component{
render(){
    return(

        
        <Button style={{ marginLeft: "auto" }}>Add Campaign</Button>

    );
}
}
export default Campaign;